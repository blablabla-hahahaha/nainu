server:
  port: 8082

spring:
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/nainu_agi
    username: root
    password: 016895
    pool:
      initial-size: 5
      max-size: 20

nainu-agi:
  sandbox:
    # 沙箱服务地址（编码节点经此远程执行；独立部署，不共享实例）
    base-url: http://localhost:8090

logging:
  level:
    nainu.top.agi.master: DEBUG
