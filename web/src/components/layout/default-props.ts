import routes from "@/config/routes";

/**
 * ProLayout 默认 props 配置。
 */
export default {
  route: {
    path: '/',
    routes: routes,
  },
  location: {
    pathname: '/',
  },
  appList: [
  ],
};
